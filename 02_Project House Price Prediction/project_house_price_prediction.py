# -*- coding: utf-8 -*-
"""Project_House_Price_Prediction.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1QooQQQ5g6nB-rAqI9hn-NTZvnoGXbubN

# 📊 Data Preparation & Preprocessing Exercise

## Problem Statement

In this hands-on exercise, you are required to perform **data preparation and preprocessing** using Python and Pandas on the given dataset (`HousePricePrediction.csv`).



## 📝 Task Overview

You will work through the following key stages to transform the raw dataset into a clean, machine-learning-ready format:

## 🎯 Objective

> **The goal is to prepare a clean, structured dataset that can be seamlessly used for further analysis and model building in the next hands-on exercise.**

---

## 📂 Dataset Information

| Property | Details |
| :--- | :--- |
| **File Name** | `HousePricePrediction.csv` |
| **Type** | CSV (Comma-Separated Values) |
| **Purpose** | House price prediction modeling |

---

## 🔧 Tools & Libraries Required

- **Python** (>= 3.7)
- **Pandas** – for data manipulation and analysis
- (Optional) **NumPy**, **Matplotlib**, **Seaborn** – for additional analysis

---

## ✅ Expected Outcome

By the end of this exercise, you should have:

- ✓ A cleaned DataFrame with no missing values or duplicates  
- ✓ Categorical features converted to numerical format  
- ✓ A well-documented preprocessing pipeline ready for model building  

---

> 💡 **Note:** This exercise lays the foundation for the next hands-on session where you will actually build and evaluate machine learning models on the prepared dataset.

### 1️⃣ Load & Explore the Dataset
- Load the dataset into a Pandas DataFrame
- Explore its structure (rows, columns, data types)
- Perform basic analysis to understand the data
"""

# Load the dataset into a Pandas DataFrame
from google.colab import files
uploaded = files.upload()

"""## 1️⃣ Dataset Loading"""

import pandas as pd

# Load dataset
df = pd.read_csv("HousePricePrediction.csv")

# Display first 10 rows
df.head(10)

"""## 2️⃣ Data Exploration"""

# Shape of dataset (rows, columns)
print("Shape:", df.shape)

# Data types of all columns
print("\nData Types:\n", df.dtypes)

# Summary statistics of numerical features
df.describe()

"""## 3️⃣ Data Cleaning"""

# Check missing values
print("Missing values:\n", df.isnull().sum())

# Handle missing values (example: fill numeric with median, categorical with mode)
for col in df.columns:
    if df[col].dtype == "object":
        df[col].fillna(df[col].mode()[0], inplace=True)
    else:
        df[col].fillna(df[col].median(), inplace=True)

# Remove duplicates
df.drop_duplicates(inplace=True)

print("Shape after cleaning:", df.shape)

"""## 4️⃣ Feature Selection"""

print(df.columns)

# Drop unnecessary identifier column
df = df.drop("Id", axis=1)

# Preview selected features
df.head()

"""## 5️⃣ Data Preprocessing"""

# Convert categorical variables into numerical form using one-hot encoding
df_encoded = pd.get_dummies(df, drop_first=True)

# Preview processed dataset
df_encoded.head()